name: Validar histórico de IA no perfil na VM

on:
  workflow_dispatch:

jobs:
  validar:
    runs-on: [self-hosted, linux]
    timeout-minutes: 10
    steps:
      - run: echo "Validação manual desativada para não disputar o runner único com o deploy."
