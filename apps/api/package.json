{
  "name": "academia-api",
  "version": "0.4.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "migrate": "node migrate.js",
    "test": "node --test",
    "check": "node --check server.js && node --check features/accessRoutes.js && node --check lib/accessPolicy.js && node --test"
  },
  "dependencies": {
    "pg": "latest"
  }
}
