{
  "name": "academia-api",
  "version": "0.6.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "migrate": "node migrate.js",
    "test": "node --test",
    "check": "node --check server.js && node --check features/accessRoutes.js && node --check features/productToolsRoutes.js && node --check features/memberWorkspaceRoutes.js && node --check lib/accessPolicy.js && node --test"
  },
  "dependencies": {
    "pg": "latest"
  }
}
