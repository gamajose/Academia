Módulo de Treinos e Análise Inteligente Local

Arquitetura
------------
O sistema usa uma arquitetura híbrida, explicável e totalmente local:

1. O professor ou administrador solicita manualmente a análise de uma ficha.
2. O backend carrega apenas os dados necessários da academia e da ficha solicitada.
3. Dados identificáveis são removidos ou pseudonimizados antes da inferência.
4. O motor de regras calcula status, confiança, revisão humana, sinais e sugestões.
5. Quando habilitado, o backend chama o Ollama local em 127.0.0.1:11434.
6. O gemma3:1b gera somente três textos curtos: resumo, mensagem ao aluno e notas ao professor.
7. O modelo não decide status, segurança, exercícios, séries, repetições, carga ou descanso.
8. O backend valida a narrativa e combina os textos com as decisões autoritativas das regras.
9. Qualquer falha usa imediatamente a análise completa do motor de regras.
10. A análise é salva como sugestão. Nenhuma ficha é alterada automaticamente.
11. O aluno recebe somente student_message depois de aprovação explícita do profissional.

A separação entre regras e narrativa reduz o custo de CPU, evita que o modelo rebaixe
alertas objetivos e mantém o resultado utilizável quando o Ollama estiver indisponível.

Origens salvas:
- local_generative: narrativa válida produzida pelo modelo local e combinada às regras.
- rules_fallback: resultado integral das regras em desativação, indisponibilidade,
  timeout, modelo ausente, recusa, JSON inválido ou narrativa insegura.

Segurança e privacidade
----------------------
- Não há integração com OpenAI, Gemini, Anthropic, Ollama Cloud ou outro provedor.
- O serviço Ollama escuta exclusivamente em 127.0.0.1:11434.
- Somente o backend Node.js acessa o Ollama.
- Não são enviados nome, e-mail, telefone, CPF, endereço, fotos, pagamento ou
  identificadores reais do aluno ao modelo.
- Textos livres removem e-mail, telefone, CPF e links antes da inferência.
- O ID do aluno é pseudonimizado por academia.
- Logs registram apenas origem, modelo, duração, erro técnico e contagem de tokens.
- Prompts, respostas completas e informações pessoais não são registrados.
- Sinais, sugestões, status e necessidade de revisão vêm do motor de regras.
- A narrativa não pode conter HTML, links, diagnóstico, prescrição, medicamentos,
  hormônios, anabolizantes, substâncias ou dosagens.
- Dor, lesão, restrições, esforço excessivo e poucos dados nunca podem ser
  rebaixados pelo modelo e exigem revisão humana quando definidos pelas regras.

Execução controlada
-------------------
- A geração ocorre apenas mediante clique de professor/administrador autorizado.
- Não ocorre no login, abertura do painel ou sincronização.
- Uma trava no PostgreSQL permite uma única geração por vez em toda a instalação.
- A trava expira e também é liberada em finally.
- Há limite por academia/hora e intervalo mínimo por ficha.
- Timeout e tamanho de saída são limitados.
- Não há nova tentativa automática após timeout, evitando duas inferências pesadas.
- keep_alive=0 solicita o descarregamento do modelo depois da resposta concluída.
- O modelo usa temperatura zero, contexto curto e no máximo 160 tokens de saída.

Variáveis do backend
--------------------
LOCAL_TRAINING_AI_ENABLED=true
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=gemma3:1b
OLLAMA_TIMEOUT_MS=240000
OLLAMA_TEMPERATURE=0
OLLAMA_KEEP_ALIVE=0
OLLAMA_NUM_CTX=1024
OLLAMA_NUM_PREDICT=160
OLLAMA_NUM_PARALLEL=1
LOCAL_TRAINING_PROMPT_VERSION=v2-narrative

Controles adicionais opcionais:
LOCAL_TRAINING_MAX_RETRIES=0
LOCAL_TRAINING_RATE_LIMIT_PER_HOUR=10
LOCAL_TRAINING_PLAN_COOLDOWN_SECONDS=60

Instalação da VM
----------------
Usar somente o instalador oficial:
  curl -fsSL https://ollama.com/install.sh | sh

Override esperado em /etc/systemd/system/ollama.service.d/override.conf:
  [Service]
  Environment="OLLAMA_HOST=127.0.0.1:11434"
  Environment="OLLAMA_NUM_PARALLEL=1"
  Environment="OLLAMA_KEEP_ALIVE=0"
  Environment="OLLAMA_NO_CLOUD=1"

Aplicar e baixar o modelo:
  sudo systemctl daemon-reload
  sudo systemctl enable --now ollama
  ollama pull gemma3:1b

Validar:
  systemctl is-active ollama
  systemctl is-enabled ollama
  ss -ltnp | grep ':11434'
  ollama list

O modelo gemma3:4b pode permanecer instalado para testes manuais, mas não é o
modelo recomendado para esta VM sem GPU. O gemma3:1b concluiu a narrativa curta
em CPU; o schema completo com listas aninhadas excedeu o tempo operacional.

Validação operacional realizada em 19/07/2026:
- deploy da main confirmado pelo commit implantado;
- API, web e Ollama ativos;
- Ollama escutando somente em 127.0.0.1:11434;
- narrativa direta válida em aproximadamente 18 segundos;
- análise completa pela rota interna em aproximadamente 27 segundos;
- 642 tokens de entrada e 100 tokens de saída no teste controlado;
- aprovação, rejeição e visibilidade do aluno validadas;
- fallback rules_fallback validado com a IA desabilitada;
- memória após os testes: aproximadamente 1,4 GiB em uso de 11 GiB;
- swap praticamente sem uso.

Rotas
-----
POST /api/training/plans/review
  Gera uma análise para plan_id. Somente owner, admin ou profissional com
  permissão de treino.

GET /api/training/plans/reviews?plan_id=...&limit=20
  Histórico isolado pela academia.

POST /api/training/plans/review/approve
POST /api/training/plans/review/reject
  Registra a decisão humana sem alterar a ficha.

GET /api/student/training/analysis
  Retorna apenas id, plan_id, student_message e approved_at da análise aprovada
  mais recente do próprio aluno. Notas internas, sinais e sugestões não saem
  por esta rota.

Banco
-----
Migration: database/058_local_training_ai_reviews.sql

workout_ai_reviews guarda a saída estruturada, origem, modelo, versão do prompt,
confiança, necessidade de revisão, decisão humana, hash do snapshot, duração,
erro técnico e contagem de tokens.

training_ai_generation_locks implementa a trava global. A migration não remove
análises antigas e preserva o funcionamento do motor de regras.

Testes mínimos
--------------
Na API:
  npm run check

Os testes cobrem saída estruturada, narrativa curta, bloqueio de conteúdo inseguro,
minimização de dados, autoridade das regras, sucesso local, timeout, resposta HTTP
inválida, JSON incompleto, modelo ausente, fallback, trava, restrições/dor,
permissões, isolamento entre academias, aprovação, rejeição e visibilidade do aluno.

Na VM também validar:
- análise real com Ollama disponível retornando local_generative;
- análise com IA desabilitada retornando rules_fallback;
- nenhuma alteração nas tabelas da ficha;
- mensagem invisível antes da aprovação e visível depois;
- API e web saudáveis durante e depois da inferência;
- consumo de CPU e memória do ollama.

Rollback
--------
Rollback operacional, sem perder análises:

1. Definir LOCAL_TRAINING_AI_ENABLED=false no ambiente real do backend.
2. Reiniciar apenas academia-api.
3. Confirmar que uma nova análise retorna source=rules_fallback.
4. Se necessário, parar e desabilitar o Ollama:
     sudo systemctl disable --now ollama
5. Para liberar disco depois de confirmar o rollback:
     ollama rm gemma3:1b
6. Restaurar o override do systemd a partir do backup preparado antes da instalação.
7. Qualquer reversão de código deve ocorrer por novo PR.

A migration deve permanecer aplicada: ela é compatível com o fallback e preserva
o histórico. Em caso de instabilidade, desabilitar a IA local é suficiente para
manter o módulo funcionando pelo motor de regras.

Limitações
----------
- A VM executa o modelo em CPU; o tempo varia conforme o tamanho da ficha e a carga do host.
- O modelo gera explicação, não decisão autônoma.
- A qualidade depende da regularidade dos registros de execução e avaliações.
- Alterações de ficha continuam sendo uma ação separada e exclusivamente humana.
- Uma requisição HTTP cancelada por timeout pode continuar consumindo CPU no Ollama
  por alguns instantes; por isso a aplicação não repete automaticamente a chamada.
